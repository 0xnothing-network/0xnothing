# This file is intentionally minimal.
# The root-level netlify.toml at the repo root is the active Netlify configuration.
# Netlify only reads netlify.toml from the repository root (or the configured base directory).
